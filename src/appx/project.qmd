---
title: "project"
format: html
---

```{r}
library(ggplot2)
library(ggiraph)
salary_potential <- read.csv("salary_potential.csv")
regions <- read.csv("states.csv")
```

```{r}

```

```{r}

p <- ggplot(salary_potential, aes(x = mid_career_pay, y = make_world_better_percent, size = stem_percent)) +
geom_point_interactive(aes(tooltip = paste("College:", name, "<br>State:", state_name, "Stem Percentage:", stem_percent)), alpha = 0.5, color = "grey") +
theme_minimal()

girafe(ggobj = p)

```

